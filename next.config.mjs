/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "uploadthing.com",
      "utfs.io",
    ],
  },
  reactStrictMode: false,
};

export default nextConfig;
